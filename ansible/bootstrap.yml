

- hosts: all
  become: yes
  gather_facts: no

  tasks:
    - name: Install Python + SELinux + rsync
      raw: |
        dnf install -y \
          python3 \
          python3-libselinux \
          libselinux \
          rsync \
          sudo
        ln -sf /usr/bin/python3 /usr/bin/python