- hosts: all
  become: yes
  gather_facts: no

  tasks:
    - name: Install Python and required system libs
      raw: |
        dnf install -y python3 python3-libselinux sudo
        ln -sf /usr/bin/python3 /usr/bin/python