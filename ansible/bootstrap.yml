- hosts: all
  become: yes
  gather_facts: no

  tasks:
    - name: Install Python3 and required tools
      raw: |
        yum install -y python3 python3-pip rsync sudo
        alternatives --install /usr/bin/python python /usr/bin/python3 2